const initialState = {
  reservationId: 12345678,
  cost: '257€',
}

export default function reservation(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
